# 0x09. Static Libraries

This project focuses on creating static libraries in C.

## Tasks

0. Create static library libmy.a
1. Create script to build liball.a

## Requirements

- Use Betty style
- No global variables
- Compile with:
  gcc -Wall -Werror -Wextra -pedantic -std=gnu89
