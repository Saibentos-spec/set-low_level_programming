# 0x01. Variables, if, else, while

This directory contains C programs for **Task 0: Positive Anything is Better than Negative Nothing** and related exercises. All files are compiled on **Ubuntu 20.04 LTS** with:

```bash
gcc -Wall -Werror -Wextra -pedantic -std=gnu89
