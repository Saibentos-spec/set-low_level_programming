# 0x0A. argc, argv

## Description

This project introduces the use of **command line arguments** in C programming using `argc` and `argv`.

It focuses on:
- Understanding how programs receive arguments from the command line
- Handling argument counts and values
- Performing operations based on user input

All programs are compiled using:
