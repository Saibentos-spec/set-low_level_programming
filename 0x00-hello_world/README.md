**A. Hello, World**

This directory contains the first tasks of the low-level programming section.

B. **Preprocessor**

A Bash script that runs a C file through the **C preprocessor** (`gcc -E`) and saves the result in a file named `c`.

- The name of the C source file is provided via the environment variable `$CFILE`
- The output file is always named exactly `c` (no extension)

**File:** `0-preprocessor`
